#include <iostream>
using namespace std;
int main()
{
    int something(112);
    cout << "Input something in the variable something : ";
    cin >> something;
    cout << "Output result value in the variable something : " << something << endl;
    return 0;
}